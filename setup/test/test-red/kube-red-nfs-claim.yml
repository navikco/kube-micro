apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kube-red-nfs
  namespace: kube-red
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 50Gi

